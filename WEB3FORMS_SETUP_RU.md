# Настройка Web3Forms для получения заявок на email

## Как работает Web3Forms? 🤔

**Простыми словами:**
1. Пользователь заполняет форму на вашем сайте → нажимает "Отправить заявку"
2. Ваш сайт отправляет данные на сервер Web3Forms (api.web3forms.com)
3. Web3Forms проверяет ваш ключ (безопасность)
4. Web3Forms форматирует данные в письмо
5. Web3Forms отправляет письмо на ваш email
6. Вы получаете письмо с заявкой! 📧

**Всё автоматически, без бекенда!**

## Что у вас уже есть:

✅ **Код уже готов** - функция отправки создана и подключена  
✅ **Ключ Web3Forms**: `b1b4d5a7-1916-4abe-839a-d9eac3d663e5`  
✅ **Email для получения заявок**: `semenohotnieoa@gmail.com`

## Что нужно сделать (2 шага):

### Шаг 1: Создать файл `.env`

Создайте файл с именем `.env` (без расширения) в корне проекта (там же, где `package.json`).

**Содержимое файла `.env`:**
```env
VITE_WEB3FORMS_ACCESS_KEY=b1b4d5a7-1916-4abe-839a-d9eac3d663e5
VITE_WEB3FORMS_TO_EMAIL=semenohotnieoa@gmail.com
```

**Важно:**
- Файл должен называться именно `.env` (с точкой в начале)
- Не должно быть расширения (.txt, .env.txt и т.д.)
- Файл должен быть в корне проекта (рядом с package.json)

### Шаг 2: Перезапустить проект

Если проект уже запущен, остановите его (Ctrl+C) и запустите заново:

```bash
npm run dev
```

**Готово!** Теперь все заявки будут приходить на `semenohotnieoa@gmail.com`

## Что будет в письме?

Когда кто-то отправит заявку, вы получите письмо на `semenohotnieoa@gmail.com`:

```
Тема: Новая заявка с сайта СФЕРА

🎯 Новая заявка с сайта СФЕРА

👤 Родитель: Иванов Иван Иванович
👶 Ребёнок: Иванов Петр Иванович
🎂 Возраст: 5 лет
📞 Телефон: +7 (999) 123-45-67
🏫 Сад/Школа: Детский сад №1
💬 Комментарий: Хотелось бы узнать о гимнастике

⏰ Дата: 15.01.2024, 14:30:00
```

## Проверка работы

1. Заполните форму на сайте
2. Отправьте тестовую заявку
3. Проверьте почту `semenohotnieoa@gmail.com`
4. Письмо должно прийти в течение нескольких секунд

## Если письма не приходят

1. **Проверьте файл `.env`**
   - Убедитесь, что файл создан в корне проекта
   - Проверьте правильность ключа и email
   - Убедитесь, что нет лишних пробелов

2. **Проверьте, что перезапустили проект**
   - Переменные окружения загружаются только при запуске
   - Остановите проект (Ctrl+C) и запустите заново

3. **Проверьте консоль браузера**
   - Откройте DevTools (F12)
   - Посмотрите на ошибки в консоли
   - Если есть ошибка "Web3Forms настройки не найдены" - проверьте `.env`

4. **Проверьте папку "Спам"**
   - Иногда письма могут попадать в спам
   - Проверьте папку "Спам" в Gmail

## Для продакшена (на сервере)

Когда будете деплоить на сервер, создайте `.env` файл на сервере:

```bash
cd /root/sfera-deti
nano .env
```

Добавьте те же строки:
```env
VITE_WEB3FORMS_ACCESS_KEY=b1b4d5a7-1916-4abe-839a-d9eac3d663e5
VITE_WEB3FORMS_TO_EMAIL=semenohotnieoa@gmail.com
```

Затем пересоберите проект:
```bash
npm run build
```

## Готово! 🎉

После создания файла `.env` и перезапуска проекта все заявки будут автоматически приходить на `semenohotnieoa@gmail.com`!




